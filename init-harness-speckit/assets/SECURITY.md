# Security

Security posture for {{REPO_NAME}}. Read before touching auth, secrets, or
trust boundaries.

> Fill each section below. Delete the prompts once filled.

## Reporting

*How to report a vulnerability (contact, private channel), and expected
response.*

## Secrets

*Where secrets live (secret manager, env), how they are injected, and the rule:
never commit real secret values.*

## Dependency scanning

*How dependencies are scanned/updated (e.g. Dependabot, `govulncheck`, audit),
and cadence.*

## Auth and trust boundaries

*What authenticates callers, which upstreams are trusted, and where input is
validated. Name the boundary the code must not cross.*
