- Constitution compliance: the plan honors every principle in
  `.specify/memory/constitution.md` (the supreme document).
- Plan-bounded change (harness): `covers:` names every path prefix this feature
  may edit.
- Verified before done (harness): `verify:` is a real command that proves the
  feature works.
